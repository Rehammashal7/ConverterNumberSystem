<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Converter" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/brand</item>
        <item name="colorPrimaryVariant">@color/brand</item>
        <item name="colorOnPrimary">@color/background</item>

        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="ConText" parent="Widget.AppCompat.TextView">
        <item name="android:textSize">@dimen/textSize</item>
        <item name="android:padding">@dimen/padding</item>

    </style>

    <style name="editTextStyle">
        <item name="android:layout_marginHorizontal">@dimen/margin_medium</item>
        <item name="android:background">@drawable/draw</item>
        <item name="android:textSize">16sp</item>
        <item name="android:padding">@dimen/padding_medium</item>
    </style>

    <style name="btn_style">
        <item name="android:layout_margin">@dimen/small_margin</item>
        <item name="android:background">@drawable/btn</item>
        <item name="android:layout_weight">1</item>


    </style>
</resources>